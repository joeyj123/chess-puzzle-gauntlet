# Progress / Context Notes

Read this first in any new chat to pick up where things left off. Updated after
each work session.

## Project
Chess Puzzle Gauntlet — React + Vite PWA. Repo: github.com/joeyj123/chess-puzzle-gauntlet
(remote `origin`, branch `main`).

## Status
All Tier 1–4 features shipped. Session 10 completed a large bug-fix + feature push.
Everything is committed and live on Vercel **except** the final batch of changes from
Session 10 (see "Uncommitted changes" below — user was in the middle of pushing when
the chat ended).

## Uncommitted changes (Sessions 10 + 11 + 12) — run this to push everything

```
npm install
git add -A
git commit -m "Anon auth + game history DB + Stockfish perf cap + Puzzle Rush labels + GameReview summaries"
git push
```

### What's included in that commit

1. **`CLAUDE.md`** — Added permanent debug checklist (run after every fix).

2. **`vite.config.js`** — Vite plugin copies `node_modules/stockfish/src/stockfish.js`
   to `public/stockfish.js` on every `npm run dev` / `npm run build`. Served as a
   static asset so it works offline after first load.

3. **`.gitignore`** — Added `public/stockfish.js` (auto-generated by vite plugin).

4. **`package.json`** — Added `"stockfish": "^16.0.0"` and `"qrcode": "^1.5.4"`.

5. **`src/useStockfish.js`** — New hook. Manages a Stockfish Web Worker lazily.
   Exports:
   - `getBestMove(fen, depth)` — returns UCI move string
   - `analyzePosition(fen, depth)` — returns `{ score, bestMove }`
   - `setSkillLevel(0–20)`, `stop()`, `terminate()`
   - `DIFFICULTY_LEVELS` — 10 evenly-spaced levels 500→2800, each with `skill` (0–20)
     and `depth` (1–18). No movetime used — depth only.
   - `classifyMove(cpLoss)` — returns `{ label, symbol, cls }` classification object
   - `moveAccuracy(cpLoss)` — returns 0–100 accuracy score

6. **`src/ComputerChess.jsx`** — New full-screen overlay. Play vs Stockfish.
   - Setup screen: pick from 10 difficulty levels (Beginner 500 → Master 2800) + color
     (White / Black / Random)
   - Playing: same layout as LiveChess, single merged `chess-status-bar` (no overlay flash)
   - Results: win/loss/draw + "📊 Review Game" + "Play Again" + "Back to Menu"
   - Local `ComputerChessErrorBoundary` wrapping the component
   - Board theme uses `{ backgroundColor: ... }` objects (not raw strings)

7. **`src/GameReview.jsx`** — New full-screen game analysis overlay.
   - Takes `pgn` + `playerColor` props
   - Analyzes every position at depth 10 via Stockfish
   - Classifies each move: Best / Excellent / Good / Inaccuracy / Mistake / Blunder
   - Shows accuracy % per player (sigmoid formula)
   - Board replay with prev/next buttons + keyboard ←→
   - Green arrow on board showing best move when viewing mistake/blunder
   - Progress bar during analysis
   - Local `GameReviewErrorBoundary`

8. **`src/LiveChess.jsx`** — Two additions:
   - Accepts `onReviewGame` prop; "📊 Review Game" button on results screen
   - `customLightSquareStyle`/`customDarkSquareStyle` fixed to use
     `{ backgroundColor: boardTheme.light }` objects (was raw string → caused the
     "Failed to set indexed property [0] on CSSStyleDeclaration" crash)
   - Move sync fixed: all fire-and-forget `.update()` calls now have `.then(() => {})`
     so Supabase v2 actually executes them (was the reason moves weren't syncing)
   - Turn/check indicators merged into single `.chess-status-bar` element

9. **`src/MultiplayerDuel.jsx`** — Same `.then(() => {})` fix on all updates.
   Single `.chess-status-bar` (no dual-element stacking).

10. **`src/App.jsx`** — Added `computerOpen`, `reviewOpen`, `reviewPgn`, `reviewColor`
    state. Added "🤖 vs Computer" menu item. Added `<ComputerChess>` and `<GameReview>`
    overlay renders. Passes `onReviewGame` to `<LiveChess>`.

11. **`src/App.css`** — Added styles for:
    - `.duel-btn-secondary`, `.duel-btn-ghost`
    - Computer setup: `.computer-setup`, `.computer-difficulty-grid` (2-col),
      `.computer-diff-btn`, `.computer-color-btn`, etc.
    - Game review: `.review-overlay`, `.review-accuracy-bar`, `.review-move-list`,
      `.review-move-cell`, `.review-nav`, `.review-progress-bar`, etc.
    - `.chess-status-bar` (replaces old `.chess-turn-indicator` + `.chess-check-warning`)

12. **`src/QRShareCode.jsx`** — New component. QR code on a `<canvas>` via `qrcode`
    npm. CJS/ESM interop handled defensively.

13. **`src/main.jsx`** — Global ErrorBoundary reload strips URL params (`window.location.pathname`).

14. **`src/useStockfish.js`** — 10 levels, depth-based only, no movetime.

## Root causes fixed this session (for reference)

- **"Failed to set indexed property [0] on CSSStyleDeclaration"** — `customLightSquareStyle`
  and `customDarkSquareStyle` in LiveChess + MultiplayerDuel were passed raw hex strings
  (`boardTheme.light = '#f0d9b5'`). The main app correctly used `{ backgroundColor: ... }`.
  Fixed in LiveChess, MultiplayerDuel, ComputerChess, GameReview.

- **Moves not syncing in LiveChess** — Supabase JS v2 query builder is lazy: `.update().eq()`
  constructs the query but never sends it without `.then()` or `await`. All fire-and-forget
  updates now have `.then(() => {})`.

- **Turn/check indicator overlap** — Two separate DOM elements stacked when both were visible.
  Merged into one `.chess-status-bar` element that transitions between states.

## Uncommitted changes (Session 11, 2026-06-17)

All changes below are ready to commit. Run in user's terminal:

```
git add -A
git commit -m "Fix vs Computer thinking forever + add Puzzle Rush click-to-move"
git push
```

### Session 11 changes

1. **`src/useStockfish.js`** — Added 15-second timeout to `getBestMove` and
   20-second timeout to `analyzePosition`. If Stockfish doesn't respond, the
   promise resolves with `null` instead of hanging forever.

2. **`src/ComputerChess.jsx`** — When `getBestMove` returns `null` (timeout or
   worker failure), the game now falls back to a random legal move rather than
   leaving `thinking=true` forever.

3. **`src/PuzzleRush.jsx`** — Implemented full click-to-move:
   - `selectedSq` / `legalTargets` state added.
   - `onSquareClick` now selects pieces, shows legal-move dots, and triggers
     `commitMove` on destination click (re-select and deselect also handled).
   - `loadPuzzle` clears selection on every new puzzle.
   - `customSquareStyles` now merges selection highlight + legal dots on top of
     existing correct/wrong flash highlights.

4. **`src/useStockfish.js`** — `getBestMove` now accepts `movetime` (default 1500 ms).
   UCI command is `go depth N movetime M` — stops at whichever limit is hit first,
   so computer moves are capped at ≤1.5 s at all difficulty levels.

5. **`src/ComputerChess.jsx`** — Passes `movetime: 1500` to `getBestMove`.

6. **`DEBUG.md`** — Created. Full log of all bugs found + fixed across sessions.

## Session 12 changes (2026-06-17)

1. **`src/useStockfish.js`** — Depth capped at 12 for all levels. Default movetime
   reduced from 1500 → 800 ms. `classifyMove` thresholds updated: 0–5 Best, 6–10
   Excellent, 11–40 Good, 41–100 Inaccuracy, 101+ Blunder (Mistake category removed).
   New export: `getClassificationSummary(cls, cpLoss)`.

2. **`src/useAuth.js`** — New hook. Calls `signInAnonymously()` on first load if no
   session exists. Exposes `{ user, loading, isAnonymous, linkGoogle }`. `linkGoogle`
   uses `supabase.auth.linkIdentity({ provider: 'google' })`.

3. **`src/App.jsx`** — Imports and calls `useAuth`. Passes `userId` to `<ComputerChess>`.
   Settings panel: new "Account" section with "🔗 Link Google Account" button (shown
   only when user is anonymous; shows signed-in email once linked).

4. **`src/ComputerChess.jsx`** — Accepts `userId` prop. Saves completed game to
   `game_history` Supabase table (fire-and-forget, never blocks UI). Unused
   `classifyMove` import removed.

5. **`src/PuzzleRush.jsx`** — Added `getPuzzleObjective(themes)` helper. Playing screen
   now shows a `.rush-meta-bar` with the puzzle objective ("Mate in 2", "Find the Fork",
   etc.) and "Your Turn: White/Black" turn label.

6. **`src/GameReview.jsx`** — `CLASS_CONFIG` updated to remove 'mistake' (aligned with
   new thresholds). Imports `getClassificationSummary`. Each move badge now shows a
   one-sentence explanation of why the move was classified that way.

7. **`supabase/schema.sql`** — New file. Full schema for `profiles` + `game_history`
   with RLS policies and an auto-create-profile trigger. Run once in Supabase SQL Editor.
   Also: enable Anonymous Sign-In in Auth → Providers first.

8. **`src/App.css`** — `.rush-meta-bar`, `.rush-objective`, `.rush-turn-label`,
   `.review-move-badge-wrap`, `.review-move-summary`, `.link-account-btn` styles added.

## Supabase setup required for Session 12 features

Before the new auth/history features go live:
1. Supabase Dashboard → Authentication → Providers → Anonymous Sign-In → **enable**
2. Authentication → Providers → Google → configure OAuth credentials
3. SQL Editor → paste contents of `supabase/schema.sql` → Run

## Next steps
1. Run the commit above (note: `npm install` was already done in Session 10 — not needed again unless fresh clone).
2. Test vs Computer (all 10 levels), Game Review (post-computer game + post-live-chess game).
3. Verify QR code shows in host lobby for both Duel and Play Chess.
4. Future ideas:
   - Time controls / chess clock for live chess
   - Draw offers
   - Move history / notation panel
   - Adaptive difficulty in computer mode (auto-suggest level up/down after each game)
   - Wordle-style shareable result card

## Known gotchas
- User runs commands in **PowerShell 5.1** — `&&` is NOT a valid statement
  separator. Use `;` or separate lines.
- The sandbox shell has no GitHub credentials — `git push` must be run by the
  user in their own terminal.
- **Don't run `git reset`/index-modifying commands from the sandbox bash** —
  previously caused a stale `.git/index.lock`.
- Sandbox bash file mounts can be **stale/truncated** for recently-edited files —
  always verify via `Read` before trusting a sandbox build error.
- `lichess_db_puzzle.csv` (1.1GB) — gitignored, not needed unless regenerating puzzles.
- `public/stockfish.js` — gitignored, auto-copied from `node_modules/stockfish` by
  the vite plugin on every build/dev start. Must run `npm install` before first dev/build.

## Workflow going forward
- User splits work into separate task-chats to avoid context limits.
- Update this file after making changes so a fresh chat has the current picture.
- See `.claudeignore` for files to skip when exploring.
- "NEW CHAT" trigger: update PROGRESS.md + FEATURES.md always; .claudeignore /
  CLAUDE.md only if workflow-relevant things changed.
